# docker-compose.yml Dosyasını Açma

## 📝 Nano Editörü ile Açma

VPS'te docker-compose.yml dosyasını açmak için:

```bash
nano docker-compose.yml
```

**Veya tam yol ile:**

```bash
nano /var/www/bulutsaas/docker-compose.yml
```

---

## 📋 Adım Adım

### ADIM 1: Proje Dizinine Gidin

```bash
cd /var/www/bulutsaas
```

### ADIM 2: Dosyayı Açın

```bash
nano docker-compose.yml
```

**Beklenen:** Nano editörü açılacak ve dosya içeriği görünecek

---

## 🔍 Dosya Kontrolü

Dosyanın var olduğunu kontrol edin:

```bash
ls -la docker-compose.yml
```

**Beklenen:** Dosya listelenmeli

---

## 📝 Nano Editörü Komutları

- **Ctrl+O** - Dosyayı kaydet (Write Out)
- **Ctrl+X** - Çık (Exit)
- **Ctrl+W** - Arama yap (Where Is)
- **Ctrl+K** - Satırı kes (Cut)
- **Ctrl+U** - Yapıştır (Paste)
- **Ctrl+G** - Yardım (Help)

---

## ✅ Sonuç

**Komut:**

```bash
cd /var/www/bulutsaas
nano docker-compose.yml
```

**Başarılar! 🚀**

